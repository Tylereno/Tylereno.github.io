import React from 'react';
import ReactDOM from 'react-dom/client';
import SubmissionVolumes from './SubmissionVolumes.jsx';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SubmissionVolumes />
  </React.StrictMode>
);
